{
  "name": "sap2",
  "private": true,
  "version": "0.0.1",
  "type": "module",
  "description": "Survivability Position Planner v2 \u2014 schema-first, ship-empty, deterministic. See docs/SAP2_BLUEPRINT.md at repo root.",
  "engines": {
    "node": "22.22.2"
  },
  "scripts": {
    "dev": "vite",
    "typecheck": "tsc --noEmit",
    "test": "node --import tsx --test \"test/**/*.test.ts\"",
    "verify": "npm run typecheck && npm run build && npm run test",
    "build": "vite build && node --import tsx scripts/build-sw.ts && node --import tsx scripts/build-standalone.ts",
    "build:app": "vite build && node --import tsx scripts/build-sw.ts"
  },
  "dependencies": {
    "three": "0.185.0"
  },
  "devDependencies": {
    "@types/node": "22.20.1",
    "@types/three": "0.185.0",
    "happy-dom": "20.11.1",
    "tsx": "4.23.1",
    "typescript": "5.9.3",
    "vite": "8.2.0"
  }
}

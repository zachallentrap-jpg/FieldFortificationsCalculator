# HANDLING.md — interim handling guidance

**Interim, conservative-by-default. Definitive posture pending counsel review
(COUNSEL_REVIEW.md q.3, q.5).**

- **The empty app** (no fill loaded): presumed unrestricted **pending counsel
  review** — never asserted as unrestricted fact.
- **Fill files** (any completeness) and **commissioned outputs**: treat as CUI-like
  until counsel says otherwise — named personnel + aggregated doctrinal values on
  one file is the sensitive combination (aggregation question q.5). Keep on
  controlled media; clear distribution with your S-6 / information-management shop.
- **TRAINING materials**: fictitious by construction and marked FICT on every value;
  still carry the watermark and never present them as real product.
- **Air-gap transfers**: the single-file artifact's `.sha256` sidecar is the
  transfer-integrity check — verify it on the receiving machine before use.
